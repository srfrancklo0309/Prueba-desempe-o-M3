{
  "name": "eventos-spa",
  "version": "1.0.0",
  "description": "SPA para gestión de eventos con roles, usando Bootstrap, JSON Server y sessionStorage.",
  "main": "src/main.js",
  "scripts": {
    "start": "live-server public --open=public/index.html",
    "json-server": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "bootstrap": "^5.3.0"
  },
  "devDependencies": {
    "json-server": "^0.17.0",
    "live-server": "^1.2.2"
  }
} 